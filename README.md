# `@fond-of/eslint-config`

## Usage

**Install**:

```bash
yarn add --dev @fond-of/eslint-config
```

**Edit `package.json`**:

```json
{
  "eslintConfig": {
    "extends": "@fond-of/eslint-config"
  }
}
```
